# 更新日志

## v1.0.13

* 新增配置 **link_strict（只统计规范的磁力链接）**，**默认开启**：链接前缀写错的
  （manget? / 
anget? / 只发一串哈希）**一律不记录、不计数**，只统计 magnet:?xt=urn:btih:<哈希>。
  关掉则恢复 v1.0.12 的容错解析（写错也尽量按哈希归并）。
* 严格模式下被忽略的消息会打一条 INFO 日志（含原文前 80 字），方便确认是不是格式写错了。
* 测试补充 4 条（严格模式：写错前缀忽略 / 裸哈希忽略 / 规范链接通过 / 纯名字通过）。

## v1.0.12

* 修复：**链接前缀写错 / 哈希不足 40 位时解析错乱**。群里常见 #验车manget?xt=urn:btih:<32位哈希>
  （manget／
anget／漏了 magnet:），旧版本认不出链接 → 把整串链接当成名字截 20 字，
  战报里出现 manget?xt=urn:btih:5 这种条目，而且同一个磁力因为写法不同被拆成多条。
  现在：只要文本里有 xt=urn:btih:<哈希> 或独立的 **32/40 位哈希**就认（32 位 base32 自动转 hex），
  统一按哈希归并计数；清理残留文本后才取名，取不到名字就退化成哈希前 12 位。
* 测试补充 4 条（写错前缀 + 32/40 位哈希、不同前缀同一 key、带名字时照常取名）。

## v1.0.11

* 修复：插件页「日期」下拉**始终包含今天**（原来只列出有记录的日期，今天还没人验车时无法预览/推送当天空战报）。
* 修复：插件页「立即推送」**点了没反应** —— 页面运行在受限 iframe 里（沙箱没有 llow-modals），
  我原先用 window.confirm() 做二次确认会被浏览器静默拦截并返回 false，导致直接 return。
  现在改为直接执行并把结果写在提示里（同样修掉了「清空白名单」时的 confirm）；提示保留时间 4s → 8s。
* 测试补充：overview.days 必含今天、按日期倒序。

## v1.0.10

* 空战报支持「**直接套用指定图片**」：当天没人验车时，直接把图片发出去，不再现场作图。
  * 内置随包图片 `resources/empty.jpg`（就是那张蓝发鲸鱼娘 +「今天一辆车都没有，群友真是不行呢」）；
  * 换图三种方式：配置 `empty_report_image` 指向图片 → 把图片命名为 `empty.jpg` / `lazy.jpg` 放进插件数据目录 → 替换 `resources/empty.jpg`；
  * 新增配置 `empty_report_mode`（下拉）：`image` 直接发图（默认）/ `generated` 现场生成摆烂图；
  * `empty_report_text` 现在只作为随图文字，**留空 = 只发图片不带文字**；
  * `#验车榜` 与插件页「生成预览」在当天无记录时也会用这张图。

## v1.0.9

* 新增「**摆烂图**」：当天没有任何 `#验车` 记录时，定时推送照常发图——
  睡觉猫 + 主文案「**今天一辆车都没，真是怠惰呢**」（HTML 与 Pillow 两条出图路径都加了 `Zzz` 装饰）。
* `push_empty_report` 默认改为 **开启**（原来是关）。
* 新增 `empty_report_text`：空战报文案可自定义（默认即上面那句）。
* 空战报的随图文字（caption）也换成摆烂文案，不再显示「共 0 次发送 / 0 条磁力」。
* 测试补充 6 项：默认开启 / 默认文案 / 战报数据带文案 / Pillow 出图 / 空战报真的发出图片 / 随图文案正确。

## v1.0.8

* 新增 **群友榜**：记录链接的同时按**发送者**汇总，展示「谁发了多少次 #验车」。
  * 战报图下方新增「群友榜 · 今日验车次数」面板（两列排布，前三名金/银/铜名次徽章），HTML 与 Pillow 两条出图路径都支持；
  * 插件页面「战报预览与手动推送」区块新增群友榜胶囊列表（鼠标悬浮显示 QQ/ID）；
  * Web API `/stats` 同步返回 `senders`，便于二次开发。
* 新增配置：`push_show_senders`（默认开）、`push_sender_top_n`（默认 5，0 = 不显示）。
* 历史数据无需迁移：每条链接记录里本来就存了 `users`（发送者 + 次数），本次只是汇总展示。

## v1.0.7

战报图**文字可读性整体提升**（HTML 与 Pillow 两条出图路径同步调整）：

* 卡片更实：数据行/统计胶囊/标题面板的白色不透明度提高（.58 → .74 等），照片不再"透"到压住文字；
* 文字更清：资源名加深加粗（#342a3e / 600 字重）并加白色描边；日期·群名、统计标签、页脚说明统一加深；
* 排名次数胶囊与进度条提高对比（更深渐变 + 阴影）；页脚改为毛玻璃小底屏，不再直接压在照片上；
* 雾化蒙版基线抬高：`背景雾化强度` 从 5 提升到等效 ~0.14 不透明度起步（dim=14 → 0.20，dim=45 → 0.42），
  轻度雾化设置下也不再糊字；Pillow 路径用同一套换算，两条路径观感一致。

## v1.0.6

* `推送时区` 改为**下拉选择**，默认 **`Asia/Shanghai`（北京时间 UTC+8）**：
  可选 北京时间 / UTC / `+08:00` / 日本 UTC+9 / 新加坡 UTC+8 / 美东 `-05:00` / 美西 `-08:00` / 跟随服务器本地时间。
  装了插件就是北京时间 23:00 推送，VPS 是 UTC 也不用换算。
* 无 `tzdata` 的平台（Windows / macOS 未装）对常见时区**按固定偏移兜底**（如 `Asia/Shanghai` → UTC+8），
  默认值在任何平台都能生效，不再静默回退。
* 测试补充：默认值 / 下拉 labels 与 options 一一对应 / 每个选项都能解析。

## v1.0.5

* 新增配置 **`push_timezone`（推送时区）**：可填 `UTC`、`Asia/Shanghai`、`+08:00` 等；留空则维持
  「跟随服务器本地时间」。填了之后**每日推送时间**与**「一天」的划分**（记录分桶、战报日期、保留天数清理）
  都按该时区计算。
* `#验车状态` 自检新增「推送时区 → 该时区当前时间」一行；启动日志同样打印生效时区。
* `requirements.txt` 增加 `tzdata`（非 Linux 平台用 IANA 名称时区时需要）。

## v1.0.4

* 修复：文转图（HTML 路径）出图**右侧/底部露白**——poster 之前写死 `720px` 宽 + `min-height:420px`，
  而 `full_page` 截图不会小于渲染视口，视口比内容大时就出现空白条。现在 poster 改为 `width:100%` +
  `min-height:100vh`（撑满视口），并隐藏滚动条避免截图出现滚动条色带。
* 修复：`.frost`（磨砂层）与 `.bg`（背景图）是绝对定位层，按绘制顺序**盖在了普通流内容之上**——
  页脚、空态文字、「更多」提示会被白纱糊掉。现在 `.poster` 加 `isolation:isolate`，底图/磨砂/花瓣用负 `z-index`。
* 改进：页脚 `margin-top:auto` 贴底；空态居中显示。
* 提示：Pillow 路径本就按内容自适应高度，想要紧凑图可用 `render_mode=pillow`。

## v1.0.3

* 修复：文转图（HTML 路径）出图**右侧/底部露白**——poster 之前写死 `720px` 宽 + `min-height:420px`，
  而 `full_page` 截图不会小于渲染视口，视口比内容大时就出现空白条。现在 poster 改为 `width:100%` +
  `min-height:100vh`（撑满视口），并隐藏滚动条避免截图出现滚动条色带。
* 修复：`.frost`（磨砂层）与 `.bg`（背景图）是绝对定位层，按绘制顺序**盖在了普通流内容之上**——
  页脚、空态文字、`更多` 提示会被白纱糊掉（数据行因 `backdrop-filter` 形成层叠上下文才侥幸可见）。
  现在 `.poster` 加 `isolation:isolate`，底图/磨砂/花瓣用负 `z-index` 沉到内容之下。
* 改进：页脚 `margin-top:auto` 贴底；空态居中显示，短内容时版面不再"头重脚轻"。
* 提示：Pillow 路径本来就是按内容自适应高度，不会有留白；想要紧凑图可用 `render_mode=pillow`。

## v1.0.3

* 新增 `#验车状态`（群主/群管理员/AstrBot 管理员）：一条消息给出完整自检信息——服务器本地时间与时区、
  推送设置、白名单、**本群是否在白名单内**、今日记录数、推送会话是否可解析、今日推送状态与最近一次结果。
* 改进：每日推送**失败会自动重试**（每 5 分钟一次，最多 3 次），只有「至少一个群推送成功」才把当天标记完成。
* 改进：白名单拦截记录时打印一次 INFO 日志（每天每群一次），便于发现「群号写错」。
* 文档：常见问题重写为排查清单（群号、时区、空记录、离线、会话解析、重试策略）。

## v1.0.2

* 修复：「启用验车记录」开关被当成总闸，关掉后**每日定时推送、`#验车榜`、`#验车推送` 一起失效**。
  现在 `enabled` 只控制「记录」这一步；推送与查询指令不受影响。
* 修复：`#验车推送` 之前只认 AstrBot 管理员，导致**群主/群管理员被拒**（AstrBot 的 `admins_id` 与 QQ 群主是两套体系）。
  现在允许 AstrBot 管理员 / 群主 / 群管理员；拿不到群成员信息的平台不拦截。
* 新增日志：启动时打印「记录开关 / 推送开关 / 推送时间 / 服务器本地时间与时区 / 白名单」。
* 配置项 `enabled` 的说明文案同步更正。

## v1.0.1

* 修复：深色主题下插件页面文字对比度不足（`html.bg-on` 误把 `--text`/`--muted` 固定为浅色主题的深灰）。
* 新增：`tests/pack_zip.py` 一键打包脚本（自动排除 `.git`、`__pycache__`、`tests/out/`、`*.zip`）。

## v1.0.0

首个版本。记录 `#验车` 后的磁力名称（前 20 字）、按群按天计数、每晚定时推送二次元战报图、
WebUI 群白名单页、背景图毛玻璃效果（HTML 文转图 + Pillow 双路径）。
