# 更新日志

## v1.0.3

* 新增 `#验车状态`（群主/群管理员/AstrBot 管理员）：一条消息给出完整自检信息——服务器本地时间与时区、
  推送设置、白名单、**本群是否在白名单内**、今日记录数、推送会话是否可解析、今日推送状态与最近一次结果。
  面向「推送没反应」的排查场景，不用再翻日志。
* 改进：每日推送**失败会自动重试**（每 5 分钟一次，最多 3 次），只有「至少一个群推送成功」才把当天标记完成；
  之前是「先标记再推送」，一次抖动就会导致当天不再推送。
* 改进：白名单拦截记录时打印一次 INFO 日志（每天每群一次），包含白名单内容与提示，便于发现「群号写错」。
* 文档：常见问题重写为排查清单（群号、时区、空记录、离线、会话解析、重试策略）。

## v1.0.2

* 修复：「启用验车记录」开关被当成总闸，关掉后**每日定时推送、`#验车榜`、`#验车推送` 一起失效**。
  现在 `enabled` 只控制「记录」这一步；推送与查询指令不受影响（当天没记录时是否发空战报由 `push_empty_report` 决定）。
* 修复：`#验车推送` 之前只认 AstrBot 管理员，导致**群主/群管理员被拒**。
  AstrBot 的 `admins_id`（全局管理员）与 QQ 群主是两套体系；现在允许 AstrBot 管理员 / 群主 / 群管理员，
  拿不到群成员信息的平台不拦截。
* 新增日志：启动时打印「记录开关 / 推送开关 / 推送时间 / 服务器本地时间与时区 / 白名单」；
  每日推送与跳过原因（无记录、无推送目标、发送结果明细）都会写日志。
* 配置项 `enabled` 的说明文案同步更正。

## v1.0.1

* 修复：深色主题下插件页面文字对比度不足。
  `html.bg-on` 曾把 `--text` / `--muted` 固定为浅色主题的深灰，导致深色主题下「深灰字 + 深色毛玻璃卡片」几乎不可读；
  现在只覆盖玻璃面相关变量（`--card` / `--bg` / `--line` / `--accent-soft`），深色主题单独定义文字色与输入框底色。
* 新增：`tests/pack_zip.py` 一键打包脚本（自动排除 `.git`、`__pycache__`、`tests/out/`、`*.zip`）。

## v1.0.0

首个版本。

* 记录群内 `#验车` 指令后的磁力名称（只取前 20 字，可配置）。
* 按「群 + 日期 + 链接」统计同一磁力链接当天的发送次数，并记录参与成员。
* 每晚 23:00（可配置）自动推送二次元风格战报图，支持 `#验车榜` 即时查看、`#验车推送` 手动补发。
* WebUI 插件页面：群白名单管理、推送时间/门槛/条数、出图方式、战报预览与立即推送。
* 背景图 + 毛玻璃效果：HTML 文转图与 Pillow 本地绘制两条路径都支持，模糊/雾化强度可实时调节。
